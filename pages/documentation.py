import streamlit as st

st.set_page_config(page_title="Документация", page_icon="📄")

def show_documentation():
    """Функция для отображения документации приложения"""
    
    st.title("📚 Документация по приложению прогнозирования финансовых показателей")
    st.markdown("""
    Это приложение предназначено для прогнозирования финансовых показателей компании на основе исторических данных.
    """)
    
    with st.expander("📌 Общее описание решения"):
        st.markdown("""
        ### Общее описание решения
        
        Приложение предоставляет два алгоритма прогнозирования:
        
        1. **BASE** - базовый алгоритм, использующий только исторические значения целевых показателей
        2. **BASE+** - расширенный алгоритм, учитывающий дополнительные факторы и взаимосвязи между показателями
        
        Оба алгоритма используют ансамбль моделей машинного обучения для повышения точности прогнозов.
        """)
    
    with st.expander("📂 Входные данные"):
        st.markdown("""
        ### Требования к входным данным
        
        На вход приложение принимает Excel-файл (.xlsm, .xlsx) со следующими характеристиками:
        
        - Данные должны быть представлены в транспонированном виде (месяцы по строкам, статьи по столбцам)
        - Первая строка содержит названия статей
        - Первый столбец содержит даты в формате, распознаваемом pandas
        - Обязательные столбцы:
            - Дата (название настраивается в конфиге)
            - Курс USD (название настраивается в конфиге)
        
        ### Преобразования входных данных:
        
        1. Транспонирование таблицы
        2. Конвертация валютных статей в рубли по курсу USD
        3. Очистка названий столбцов (удаление лишних символов)
        4. Заполнение пропущенных значений нулями
        5. Преобразование типов данных
        """)
    
    with st.expander("⚙️ Алгоритмы работы"):
        st.markdown("""
        ### Базовый алгоритм (BASE) без использования фичей
        
        1. **Наивный прогноз** - простое копирование последнего известного значения
        2. **Прогноз временных рядов** с использованием:
           - AutoGluon TimeSeries (ансамбль моделей)
           - AutoARIMA
           - TemporalFusionTransformer (TFT)
           - PatchTST
           - Chronos (pretrained model)
        3. **Стекинг моделей**:
           - SVR с окнами 6, 9, 12 месяцев - [predict_naive, predict_TS_ML]
           - Линейная регрессия с окнами 6, 9, 12 месяцев - [predict_naive, predict_TS_ML]
           - Random Forest для финального агрегирования прогнозов - [predict_naive, predict_TS_ML, predict_svm9, predict_linreg9_no_bias]
        
        ### Расширенный алгоритм (BASE+) с исползованием фичей
        
        1. **Учет дополнительных факторов**:
           - Конвертация выбранных статей-фич в USD
           - Добавление лаговых переменных
           - Разложение даты на год и месяц
        2. **Наивный прогноз** - простое копирование последнего известного значения
        3. **Прогноз временных рядов** с использованием:
           - AutoGluon TimeSeries (ансамбль моделей)
        4. **Дополнительные признаки для табличного кейса**:
           - Скользящие средние, минимумы и максимумы за окно в последние 6, 9, 12 месяцев
        5. **Tabular модели**:
           - AutoGluon TabularPredictor
           - TABPFNMIX (специальная модель для табличных данных)
        6. **Стекинг моделей**:
           - SVR с окнами 6, 9, 12 месяцев - [predict_naive, predict_TS_ML, predict_ML_tabular, predict_TABPFNMIX]
           - Линейная регрессия с окнами 6, 9, 12 месяцев - [predict_naive, predict_TS_ML, predict_ML_tabular, predict_TABPFNMIX]
           - Random Forest для финального агрегирования прогнозов - [predict_naive, predict_TS_ML, predict_ML_tabular, predict_TABPFNMIX, predict_svm9, predict_linreg9_no_bias]
        """)
    
    with st.expander("📊 Выходные данные"):
        st.markdown("""
        ### Структура выходного файла
        
        Результаты сохраняются в Excel-файл со следующими листами:
        
        1. **data** - основные результаты прогнозирования:
           - Фактические значения
           - Прогнозы всех моделей
           - Ошибки прогнозов (абсолютные и относительные)
        
        2. **coeffs_with_intercept** - веса линейных моделей с intercept
        
        3. **coeffs_no_intercept** - веса линейных моделей без intercept
        
        4. **TimeSeries_ensemble_models_info** - информация о весах ансамбля временных рядов
        
        5. **Tabular_ensemble_models_info** - информация о весах ансамбля табличных моделей (только BASE+)
        
        6. **Tabular_feature_importance** - важность признаков для табличных моделей (только BASE+)
        """)
    
    with st.expander("🛠 Технические детали"):
        st.markdown("""
        ### Используемые технологии
        
        - **Streamlit** - веб-интерфейс
        - **AutoGluon** - автоматическое машинное обучение
        - **Scikit-learn** - классические ML модели
        - **Pandas/Numpy** - обработка данных
        
        ### Конфигурация
        
        **Для технических юзеров есть возможность изменения конфигурационного файла посредством загрузки его новой, измененной версии**

        Настройки приложения хранятся в файле `config_refined.yaml`:

        - Названия ключевых столбцов
        - Список статей и их фичей
        - Настройки моделей
        - Параметры преобразований
        
        ### Логика работы
        
        1. Пользователь загружает файл с данными
        2. Выбирает алгоритм (BASE/BASE+)
        3. Выбирает статьи для прогнозирования
        4. Указывает месяц прогноза
        5. Запускает расчет
        6. Получает файл с результатами
        """)

show_documentation()